name: Deploy to GitHub Pages with Firebase Config

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        
    - name: Inject Firebase Configuration
      run: |
        # Verificar se os secrets estão definidos
        echo "🔍 Verificando secrets do Firebase..."
        if [ -z "${{ secrets.FIREBASE_API_KEY }}" ]; then
          echo "❌ FIREBASE_API_KEY não definido"
          exit 1
        fi
        
        # Criar configuração inline no HTML usando Python
        echo "📝 Injetando configuração Firebase diretamente no HTML..."
        
        python3 << 'EOF'
        import re
        
        # Ler o arquivo HTML
        with open('presenciais/presenciais.html', 'r', encoding='utf-8') as f:
            html_content = f.read()
        
        # Remover referência ao firebase-config-local.js (não existe em produção)
        html_content = html_content.replace(
            '<script src="firebase-config-local.js"></script>\n    ',
            ''
        )
        html_content = html_content.replace(
            '<script src="firebase-config-local.js"></script>',
            ''
        )
        
        # Criar o script de configuração
        firebase_script = '''<script>
        // Configuração Firebase injetada pelo GitHub Actions
        window.FIREBASE_API_KEY = '${{ secrets.FIREBASE_API_KEY }}';
        window.FIREBASE_AUTH_DOMAIN = '${{ secrets.FIREBASE_AUTH_DOMAIN }}';
        window.FIREBASE_PROJECT_ID = '${{ secrets.FIREBASE_PROJECT_ID }}';
        window.FIREBASE_STORAGE_BUCKET = '${{ secrets.FIREBASE_STORAGE_BUCKET }}';
        window.FIREBASE_MESSAGING_SENDER_ID = '${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}';
        window.FIREBASE_APP_ID = '${{ secrets.FIREBASE_APP_ID }}';
        console.log('🔐 Variáveis Firebase carregadas via GitHub Actions');
        </script>'''
        
        # Substituir ou inserir o script
        if 'firebase-config-universal.js' in html_content:
            # Inserir depois do firebase-config-universal.js
            html_content = html_content.replace(
                '<script src="firebase-config-universal.js"></script>',
                '<script src="firebase-config-universal.js"></script>\n    ' + firebase_script
            )
        else:
            # Inserir antes do firebase-config-safe.js
            html_content = html_content.replace(
                '<script src="firebase-config-safe.js"></script>',
                firebase_script + '\n    <script src="firebase-config-safe.js"></script>'
            )
        
        # Salvar o arquivo modificado
        with open('presenciais/presenciais.html', 'w', encoding='utf-8') as f:
            f.write(html_content)
        
        print("✅ Configuração Firebase injetada com sucesso")
        EOF
        
    - name: List files before deploy
      run: |
        echo "📂 Estrutura do projeto antes do deploy:"
        find . -type f -name "*.html" -o -name "*.js" -o -name "*.css" | head -20
        echo ""
        echo "📋 Arquivos na pasta presenciais:"
        ls -la presenciais/
        
    - name: Setup Pages
      uses: actions/configure-pages@v4
      
    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: ./
        
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
        
    - name: Success notification
      run: |
        echo "✅ Deploy realizado com sucesso!"
        echo "🔐 Configuração Firebase injetada"
        echo "🌐 Site disponível em: ${{ steps.deployment.outputs.page_url }}"