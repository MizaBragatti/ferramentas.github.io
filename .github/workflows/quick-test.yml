name: 🔍 Quick Secret Test

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  check-secrets:
    runs-on: ubuntu-latest
    steps:
    - name: Check Firebase Secrets
      run: |
        echo "🔍 Verificação Rápida das Secrets Firebase"
        echo "========================================"
        
        if [ -n "${{ secrets.FIREBASE_API_KEY }}" ]; then
          echo "✅ FIREBASE_API_KEY: OK"
        else
          echo "❌ FIREBASE_API_KEY: FALTANDO"
        fi
        
        if [ -n "${{ secrets.FIREBASE_PROJECT_ID }}" ]; then
          echo "✅ FIREBASE_PROJECT_ID: OK (${{ secrets.FIREBASE_PROJECT_ID }})"
        else
          echo "❌ FIREBASE_PROJECT_ID: FALTANDO"
        fi
        
        echo "========================================"
        echo "🔗 Configure secrets em:"
        echo "https://github.com/${{ github.repository }}/settings/secrets/actions"